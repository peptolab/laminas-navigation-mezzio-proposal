<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 * @var string $content
 * @var string $role
 */
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= $this->escapeHtml($pageTitle ?? 'Mezzio Navigation Demo') ?></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .sidebar {
            min-height: calc(100vh - 56px);
            background-color: #f8f9fa;
            border-right: 1px solid #dee2e6;
        }
        .sidebar a {
            color: #333;
            padding: 0.5rem 1rem;
            border-radius: 0.25rem;
            margin: 2px 0;
            display: block;
            text-decoration: none;
        }
        .sidebar a:hover {
            background-color: #e9ecef;
        }
        .sidebar li.active > a {
            background-color: #0d6efd;
            color: white;
        }
        .sidebar ul {
            list-style: none;
            padding-left: 0;
            margin: 0;
        }
        .sidebar ul ul {
            padding-left: 1rem;
        }
        .sidebar ul ul a {
            font-size: 0.9rem;
        }
        .sidebar ul ul ul a {
            font-size: 0.85rem;
        }
        .nav-section {
            margin-bottom: 1rem;
        }
        .nav-section-title {
            font-size: 0.75rem;
            text-transform: uppercase;
            color: #6c757d;
            padding: 0.5rem 1rem;
            font-weight: bold;
        }
        .breadcrumb-container {
            background-color: #f8f9fa;
            padding: 0.75rem 1rem;
            margin-bottom: 1rem;
            border-radius: 0.25rem;
        }
        .role-switcher {
            background-color: #e9ecef;
            padding: 1rem;
            border-radius: 0.25rem;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Mezzio Navigation Demo</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            Current Role: <span id="currentRole"><?= $this->escapeHtml($role) ?></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="?role=guest">Guest</a></li>
                            <li><a class="dropdown-item" href="?role=member">Member</a></li>
                            <li><a class="dropdown-item" href="?role=admin">Admin</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar Navigation -->
            <nav class="col-md-3 col-lg-2 d-md-block sidebar collapse pt-3">
                <div class="role-switcher">
                    <small class="text-muted">ACL Demo - Current Role:</small>
                    <div class="btn-group btn-group-sm mt-2 w-100" role="group">
                        <a href="?role=guest" class="btn btn-outline-secondary <?= $role === 'guest' ? 'active' : '' ?>">Guest</a>
                        <a href="?role=member" class="btn btn-outline-secondary <?= $role === 'member' ? 'active' : '' ?>">Member</a>
                        <a href="?role=admin" class="btn btn-outline-secondary <?= $role === 'admin' ? 'active' : '' ?>">Admin</a>
                    </div>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Navigation</div>
                    <?= $this->navigation('default')->menu() ?>
                </div>
            </nav>

            <!-- Main content -->
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 py-3">
                <!-- Breadcrumb -->
                <div class="breadcrumb-container">
                    <?= $this->navigation('default')->breadcrumbs()->setMinDepth(0)->setLinkLast(false) ?>
                </div>

                <!-- Page content -->
                <?= $this->content ?>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>